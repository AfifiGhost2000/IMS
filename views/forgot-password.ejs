<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forgot Password</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <style>
        /* Reset some default margin and padding on the body and header */
        body, header {
            margin: 0;
            padding: 0;
            background: #F2F1F3;

        }

        header {
            width: 1279.002px;
            text-align: center; /* Center-align the heading */
            padding: 20px 50px 60px 80px; /* Add some padding to the top and bottom of the header */
        }

        h1 {         
            width: 100%;
            height: 100%;
            flex-shrink: 0;
            color: #000;
            position: relative;
            left: 50%; /* Move it 50% from the left edge of the header */
            transform: translateX(-50%); /* Center it horizontally */
            margin-top: 10px;
            margin-bottom: 0px; /* Add some margin to the bottom */
            text-align: center;
            font-family: Inter;
            font-size: 38px;
            font-style: normal;
            font-weight: 700;
            line-height: normal;
            
        }
        .header-top {
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: relative; /* Position this element relative within the header */
            top: 0; /* Place it at the top */
            left: 0; /* Place it at the left */
            right: 0; /* Place it at the right */
            padding: 10px 20px; /* Add some padding to the top bar */
        }

        .icon {
            font-size: 24px; /* Adjust the font size for your icon */
            color: #554D56; /* Color of the icon */
        }

        .login-register {
            text-align: right;
            font-size: 14px; /* Adjust the font size for your text */
        }

        .login-register a {
            text-decoration: none;
            color: #554D56; /* Color of the text links */
            margin-left: 10px; /* Add some spacing between the links */
        }


        .horizontal-line {
            width: 1279.002px; /* Make the line span the entire width of the header */
            height: 2px;
            background: #554D56; /* Color of the horizontal line */
        }
        label {
            display: block;
            font-weight: bold;
            width: 148px;
            height: 45px;
            flex-shrink: 0;
            color: #000;
            font-family: Inter;
            font-size: 22px;
            font-style: normal;
            font-weight: 400;
            line-height: normal;

        }
        button {
            background: #554D56;
            width: 156px;
            height: 47px;
            flex-shrink: 0;
            color: #FFF;
            text-align: center;
            font-family: Inter;
            font-size: 20px;
            font-style: normal;
            font-weight: 400;
            line-height: normal;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            display: block; /* Make the button a block-level element */
            margin: 0 auto; /* Center the button horizontally */
        }

        button:hover {
            background-color: #0056b3;
        }
        .spacer {
        margin-bottom: 20px; /* Adds space below the element */
        }
    </style>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script>

        // $(document).ready(function() {
        //     // Function to handle the button behavior
        //     function handleResetPasswordButton() {
        //         // Disable the button and fade it out
        //         $('#reset-password-button').prop('disabled', true).fadeOut();

        //         // Start a timer to re-enable the button after 1 minute (60000 milliseconds)
        //         setTimeout(function() {
        //             // Enable the button and fade it in
        //             $('#reset-password-button').prop('disabled', false).fadeIn();

        //             // Clear the message
        //             $('#message').text('');
        //         }, 60000);
        //     }

        //     // Attach the click event handler to the button
        //     $('#reset-password-button').on('click', handleResetPasswordButton);
        // });


                  
    </script>
    

    
</head>
<body>
    <header>
    
        <div class="header-top">
            <div class="icon"><i class="fas fa-user"></i> </div>
            <div class="login-register">
                <a href="login">Login</a> | <a href="register">Register</a>
            </div>
        </div>
        <div class="horizontal-line"></div>

        <div class="spacer"></div>
     
       <img src="https://www.freeiconspng.com/uploads/forgot-password-icon-6.jpg" alt="Forgot Password" />
    </header>

    <main>
        <h1>Forgot Password</h1>
        <p>Please enter your email address, and we'll send you a password reset link.</p>

        <form method="post" action="/forgot-password">
            <div class="form-group">
                <label for="email">Email:</label>
                <input type="email" id="email" name="email" placeholder="Your Email" required>
            </div>
            <button id="reset-password-button" type="submit">Send Reset Link</button>
            <div id="message"><%= message %></div>
        </form>

        <!-- Include any additional content or messages here, such as success messages or error messages -->
    </main>

    <footer>
        <!-- Include your footer content here if you have one -->
    </footer>
</body>
</html>
